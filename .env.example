# Discord OAuth & Bot Credentials (Required)
CLIENT_ID=your_discord_client_id
CLIENT_SECRET=your_discord_client_secret
BOT_TOKEN=your_discord_bot_token

# Database Connection (Required)
MONGO_CONNECTION=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Webhook Security (Required for bot↔dashboard communication)
# Generate a random string for security using crypto module (min 32 characters)
WEBHOOK_SECRET=your_dashboard_webhook_secret

# Instatus API (Required for status monitoring)
# Get your API key from: https://dashboard.instatus.com/developer
INSTATUS_API=your_INSTATUS_API

# Server Configuration (Optional)
# Defaults to production if not set - only use 'development' for local dev
# NODE_ENV=development

# ────────────────────────────────────────────────────────────────
# NOTE: Public URLs (site URL, support server, docs, etc.) are now
# configured in src/config/site.ts instead of environment variables.
# CLIENT_ID is used to generate bot invite URLs dynamically.
# Only secrets should be in .env files.
# ────────────────────────────────────────────────────────────────